# ===== Server Configuration =====
PORT=3000
NODE_ENV=production
BASE_URL=https://your-app.railway.app

# ===== Security =====
# IMPORTANT: Generate a strong random secret key for production
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-secret-key-change-this-in-production

# ===== Database =====
DB_PATH=./data/data.db
PARTICIPANT_CSV=./data/participantes.csv

# ===== Email Configuration =====
EMAIL_USER=your-email@example.com
EMAIL_PASS=your-email-password
RESEARCH_EMAIL=researcher@example.com
# Email opcional para recibir copias de emails enviados a participantes
RESEARCH_CC_EMAIL=cc-researcher@example.com

# ===== Admin Access =====
ADMIN_USER=admin
ADMIN_PASS=change-this-password

# ===== Backups - Dropbox Configuration =====
# IMPORTANTE: Dropbox usa tokens de corta duración que expiran en unas horas.
# Por eso necesitamos un REFRESH TOKEN que permite renovar automáticamente el access token.
#
# Pasos para obtener las credenciales de Dropbox:
# 1. Ir a https://www.dropbox.com/developers/apps
# 2. Create app → Scoped access → Full Dropbox
# 3. Dale un nombre a tu app (ej: "ActPrion Backups")
# 4. En la página de Settings de tu app, encontrarás:
#    - App key (DROPBOX_APP_KEY)
#    - App secret (DROPBOX_APP_SECRET) - Click en "Show" para verlo
# 5. En la sección "OAuth 2" → "Generated access token":
#    - Click en "Generate" para generar un access token
#    - IMPORTANTE: Haz clic en el botón que dice "Get refresh token" o
#      verifica que tu app tenga habilitado "Access token expiration"
# 6. Usa el código de autorización para obtener el refresh token
#    (ver documentación en docs/DROPBOX_REFRESH_TOKEN.md)
#
# Configuración requerida para backups automáticos:
DROPBOX_REFRESH_TOKEN=
DROPBOX_APP_KEY=
DROPBOX_APP_SECRET=

# Sistema de backups (3 capas):
# Capa 1: Backups locales - Diario 2:00 AM (retención 7 días)
# Capa 2: CSV a Dropbox - Diario 3:00 AM (requiere credenciales)
# Capa 3: DB completa - Domingos 4:00 AM (requiere credenciales)

# ===== Optional =====
SUBMISSION_TOKEN=
